<UserControl x:Class="kiosk_wpf.App.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Event Key Filter -->
        <TextBlock Grid.Row="0"
                   Text="Event Key Filter"
                   FontWeight="SemiBold"
                   Margin="0,0,0,4" />

        <TextBox x:Name="EventKeyBox"
                 Grid.Row="1"
                 Height="32"
                 Margin="0,0,0,16"
                 TextChanged="OnSettingChanged" />

        <!-- Calculator Options -->
        <TextBlock Grid.Row="2"
                   Text="Calculator Options"
                   FontWeight="SemiBold"
                   Margin="0,0,0,4" />

        <CheckBox x:Name="VerboseLogging"
                  Grid.Row="3"
                  Content="Verbose Logging"
                  Margin="0,0,0,16"
                  IsChecked="True"
                  Checked="OnSettingChanged"
                  Unchecked="OnSettingChanged" />

        <!-- Mode Selection -->
        <ComboBox x:Name="OptionBCombo"
                  Grid.Row="4"
                  Height="32"
                  SelectionChanged="OnSettingChanged">
            <ComboBoxItem Content="Mode 1" />
            <ComboBoxItem Content="Mode 2" />
            <ComboBoxItem Content="Mode 3" />
        </ComboBox>

    </Grid>
</UserControl>