/**
This page NEEDS:

1. Quick Links

Frontend URL
Backend URL
Vercel deployment
Render deployment
GitHub repository
Neon database console

2. Storage Inspector

LocalStorage: list keys and values, clear individual or all, export all
Cookies: list name/value pairs, clear all
Dexie: list tables with record counts, preview contents, export, clear
    (maybe use react-json-view)

3. Deployment and Build Info

Frontend: branch name, commit ID, commit message, deploy time, environment
Backend: same info fetched from /version endpoint(create new endpoint)

4. Latency Tester

Upload to server time
Download from server time
DB upload time (from backend response)
DB download time (from backend response)
**/
export default function DevPage() {return (<></>)}